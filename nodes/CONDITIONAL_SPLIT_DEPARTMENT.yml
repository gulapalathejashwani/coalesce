version: 1
node_type: transform
name: CONDITIONAL_SPLIT_DEPARTMENT
source: SRC_DEPARTMENT
sql: |
  SELECT DEPT_ID, DEPT_NAME, LOCATION
  FROM SRC_DEPARTMENT
  WHERE LOCATION = 'Chennai'
columns:
  - name: DEPT_ID
  - name: DEPT_NAME
  - name: LOCATION
