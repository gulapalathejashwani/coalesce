fileVersion: 1
id: BCTL_ACCOUNTTYPE_SRC
name: SOURCE_BCTL_ACCOUNTTYPE
operation:
  config:
    clusterKey: false
    downstreamOption: true
    groupByAll: false
    initialize: ON_CREATE
    insertStrategy: INSERT
    refresh_mode: AUTO
    secureoption: false
    selectDistinct: false
    warehouseName: PC_COALESCE_WH
  database: PC_COALESCE_DB
  deployEnabled: true
  description: Source table BCTL_ACCOUNTTYPE from GuideWire database
  isMultisource: false
  locationName: SOURCE
  materializationType: source
  metadata:
    appliedNodeTests: []
    columns:
      - columnReference:
          columnCounter: 1
          stepCounter: BCTL_ACCOUNTTYPE_SRC
        dataType: NUMBER(38,0)
        description: Primary Key ID
        name: ID
        nullable: false
      - columnReference:
          columnCounter: 2
          stepCounter: BCTL_ACCOUNTTYPE_SRC
        dataType: VARCHAR(256)
        description: Account Type Name
        name: NAME
        nullable: true
    cteString: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - customSQL:
          customSQL: ""
        dependencies:
          - locationName: SOURCE
            nodeName: BCTL_ACCOUNTTYPE
        join:
          joinCondition: FROM {{ ref('SOURCE', 'BCTL_ACCOUNTTYPE') }}
        name: BCTL_ACCOUNTTYPE_SRC
  name: SOURCE_BCTL_ACCOUNTTYPE
  overrideSQL: false
  schema: RAW
  sqlType: SOURCE
  type: sql
  version: 1