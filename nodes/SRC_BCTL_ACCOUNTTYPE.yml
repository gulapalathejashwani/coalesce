fileVersion: 1
id: SRC_BCTL_ACCOUNTTYPE
name: SRC_BCTL_ACCOUNTTYPE
type: source
config:
  database: PC_COALESCE_DB
  schema: RAW
  table: BCTL_ACCOUNTTYPE
columns:
  - name: ID
    dataType: INTEGER
    nullable: true
  - name: NAME
    dataType: VARCHAR(50)
    nullable: true