fileVersion: 1
id: src_bctl_accounttype_node
name: SRC_BCTL_ACCOUNTTYPE
description: Source node for BCTL_ACCOUNTTYPE table
type: source
config:
  database: PC_COALESCE_DB
  schema: RAW
  table: BCTL_ACCOUNTTYPE
columns:
  - name: ID
    dataType: INTEGER
    nullable: true
  - name: NAME
    dataType: VARCHAR(50)
    nullable: true