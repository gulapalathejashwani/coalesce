fileVersion: 1
id: SRC_BCTL_STATE
name: SRC_BCTL_STATE
type: source
config:
  database: PC_COALESCE_DB
  schema: RAW
  table: BCTL_STATE
columns:
  - name: ID
    dataType: INTEGER
    nullable: true
  - name: NAME
    dataType: VARCHAR(50)
    nullable: true